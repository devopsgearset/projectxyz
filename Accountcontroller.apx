
public class AccountsController
{

	public List<Account> getAllAccounts(){
		List<Account> lstAccount=[SELECT Id,Name FROM Account];	
		system.debug('Account List to display citities based'+); //change from vscode
		system.debug('Account::'+lstAcount);
		return lstAccount;
	}
}
